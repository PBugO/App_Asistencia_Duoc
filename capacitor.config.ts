import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'APP_Asistencia_DUOC',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
